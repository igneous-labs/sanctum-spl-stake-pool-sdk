{
  "name": "tests",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "start:infra": "docker compose -f ../../docker-compose-local-validator.yml up -d",
    "stop:infra": "docker compose -f ../../docker-compose-local-validator.yml down -v"
  },
  "dependencies": {
    "@sanctumso/spl-stake-pool": "file:../sdk/pkg",
    "@solana/kit": "^2.1.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3",
    "@types/node": "^22",
    "vitest": "^2"
  }
}
